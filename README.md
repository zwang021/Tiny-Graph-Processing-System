# Tiny-Graph-Processing-System
The graph processing system supports two different algorithms, Single Source Shortest Path (SSSP) and Page Rank. The engine processes the graph in parallel using pthreads.
